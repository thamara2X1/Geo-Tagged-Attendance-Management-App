name: geo_attendance
description: Geo-tagged attendance management app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  geolocator: ^13.0.1
  intl: ^0.19.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true